//checkButton

import { makeStyles } from "@material-ui/core/styles";

let useStyle = makeStyles((theme) => ({
  checkButton: {
    color:"green"
  },
  deleteButton:{
      color:"red"
  },
  paymentButton:{
      color:"darkred"
  }
  
}));

export default useStyle;