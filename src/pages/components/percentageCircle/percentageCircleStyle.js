import { makeStyles } from '@material-ui/core';

let useStyle = makeStyles((theme) => ({
    root:{
        color: "#0077b6",
    },
    textStyle:{
        color: "#0077b6",
        fontSize:"3em"
    }
}))

export default useStyle